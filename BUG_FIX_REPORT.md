# 拾味堂三端 Bug 排查与修复报告

排查范围：FastAPI 后端、Vue 3 管理端、uni-app H5/微信小程序、数据库初始化、生产配置和三端核心业务流程。

## 结论

本轮已处理审计中发现的明显功能、并发、安全、部署和移动端问题。最新源码通过后端编译与测试、三份生产构建、SQLite 完整性检查以及桌面/移动浏览器真实流程验收；测试范围内未再发现阻断验收的明显 Bug。

## 后端修复

### 微信与生产安全

- 恢复并重写被截断的微信服务模块。
- 微信支付 v2 使用 HMAC-SHA256，请求、预支付响应和支付通知均校验签名与关键字段。
- 生产模式不再因凭证缺失静默退回模拟模式。
- `DEV_MODE=false` 时强制校验四项微信配置、强 `SECRET_KEY`、公网 HTTPS `NOTIFY_URL` 和 `PAYMENT_CLIENT_IP`。
- 微信登录异常不再把 `WX_SECRET` 暴露给客户端或日志。
- 取消或过期真实订单前先调用微信关单；金额不符、签名错误和已取消订单的通知不会错误确认成功。

### 订单、支付与并发

- 下单、订单项写入和购物车清空保持同一事务，避免孤儿订单。
- 同一用户结算加锁，MySQL 购物车快照使用 `FOR UPDATE`，降低重复订单和并发加购丢失风险。
- 购物车加购使用数据库原子累加，单菜数量限制为 99。
- 支付、取消和完成使用带前置状态的条件更新，防止陈旧会话覆盖新状态。
- 已取消订单不会被支付通知重新激活；支付和取消竞争有独立回归测试。
- 打包遵循主 PRD，表示到店带走，不强制填写地址。
- 过期订单在用户列表、详情、管理列表和看板访问路径中统一懒关闭。

### 数据、时区与输入

- SQLite 每个连接启用外键，并为旧数据库补齐分类名称唯一索引。
- 数据库存储 UTC；展示、日期筛选、“今日订单”和“今日销售额”按 `APP_TIMEZONE` 转换。
- Windows 缺少 tzdata 时，`Asia/Shanghai` 有明确 UTC+8 回退。
- 看板状态使用数字值，并返回完整 `status_counts`。
- 分类、菜品、购物车和用户输入增加空白、长度、价格与数量校验。
- 管理员登录失败限流只统计失败请求，成功登录会清除失败记录。

### 初始化资源

- 14 张菜品图片已纳入 `backend/seed_assets/dishes`，不再依赖被忽略的本机上传目录或当前月份。
- `seed.py` 将图片复制到固定 `/static/dishes/seed/` 路径；资源缺失时不写入必然 404 的 URL。
- 静态上传目录在挂载前创建，首次启动不会因目录不存在而失败。

## 管理端修复

- API 基址通过 `VITE_API_BASE` 配置，开发环境继续使用同源代理。
- 订单和菜品筛选增加请求代际保护，旧响应不能覆盖新筛选结果。
- 看板直接使用后端完整状态统计，不再由分页列表推算。
- 图片上传具有表单代际保护；上传中禁止保存或关闭，迟到响应不能污染新表单。
- 上下架状态以服务端响应为准。
- 统一错误提示，避免拦截器与页面重复 Toast。
- 移动抽屉补充退出入口。
- 351px 订单详情弹窗改为单列信息布局，标签不再逐字竖排，弹窗和页面均无横向溢出。
- 用户端和管理端均补充品牌 favicon，浏览器不再产生 favicon 404。

## 用户端修复

- H5 使用同源 API，小程序开发和生产分别使用可配置绝对地址；生产产物不再内置假域名。
- 401 自动重新登录一次并重放原请求，登录刷新模块消除了静态循环依赖。
- H5 开发环境为每个浏览器生成独立 OpenID，避免共享购物车和订单。
- 登录 Promise 使用认证代际和 Promise 身份检查；退出会立即释放旧 Promise，旧登录不能覆盖新凭证。
- 打包确认页移除地址必填。
- 订单已创建但无支付参数时跳转订单列表；微信支付成功后轮询后端状态再反馈结果。
- 修复订单按钮 Enter 冒泡进入详情、过期订单详情不刷新和首页错误状态被异常变量遮蔽。
- 首页分类请求增加竞态保护；管理端与用户端的快速筛选均不会被旧请求覆盖。
- 新增首页热门菜与个人中心，TabBar 完整覆盖首页、点餐、购物车、订单和我的。
- 修复 H5 Tab 页面回切时 `scroll-view` 节点已卸载导致的控制台异常，同时避免重复加载闪烁。

## 最终验证

| 检查 | 结果 |
| --- | --- |
| Python `compileall` | 通过 |
| `python -m unittest -v test_regressions.py` | `9/9` 通过 |
| 最新源码独立启动后 `python smoke_test.py` | `62/62` 通过 |
| 种子脚本隔离数据库连续执行两次 | 管理员 1、分类 6、菜品 14，图片 14，幂等通过 |
| SQLite `foreign_keys` / `integrity_check` | `1` / `ok` |
| SQLite 外键检查与孤儿记录 | 无异常 |
| `python -m pip check` | 无破损依赖 |
| 管理端 `npm run build` | 通过 |
| 用户端 `npm run build:h5` | 通过 |
| 用户端 `npm run build:mp-weixin` | 通过，无循环导入警告 |
| 全仓 `git diff --check` | 通过，仅存在 Git 行尾转换提示 |

### 浏览器验收

- H5 390×844：热门菜真实图片、分类定位、加购、购物车数量、无地址打包、模拟支付、订单列表、订单详情和个人中心通过。
- H5 320×568：首页、Tab 回切和横向热门菜通过；页面宽度等于视口宽度，图片均完整加载，控制台无运行时错误。
- 401 定向验证：订单请求依次得到 401、自动登录 200、原订单请求重放 200，token 已更新。
- 管理端 1440×900：登录、看板、订单详情和“标记完成”通过。
- 管理端 351×812：移动导航、退出入口、订单详情、单列标签和横向滚动表格通过；页面与弹窗无视口溢出。
- 修复后的干净会话中，管理端和 H5 均无非预期控制台错误；favicon 正常加载。

## 发布前条件与剩余风险

- 真实微信登录/支付需要用户自己的 Appid、商户凭证、HTTPS 合法域名和公网回调，本地无法代替商户环境做最终扣款验收。
- 微信小程序发布前仍需填写 DCloud Appid/微信 Appid 和生产 `VITE_API_BASE`。
- 当前完整运行态回归使用 SQLite；MySQL 代码路径、建库脚本和锁语义已审查，但仍应在目标 MySQL 环境执行一次迁移与并发验收。
- 管理端构建仍有 Sass Legacy API、第三方 PURE 注释和主 chunk 超过 500 kB 的非阻断警告，不影响当前功能正确性。
