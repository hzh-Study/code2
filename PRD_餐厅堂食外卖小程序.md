# 餐厅堂食 + 打包外卖小程序系统 PRD 产品需求文档

> 版本：V1.0  
> 状态：已评审待开发  
> 适用对象：后端开发、前端（小程序）开发、管理后台（Vue3）开发、测试、产品、运营  
> 文档性质：正式产品需求文档（PRD），可直接作为开发交付依据  

---

## 一、项目概述

### 1.1 项目背景
本项目为一套面向线下餐饮门店的「堂食 + 打包外卖」一体化微信小程序系统。门店通过本系统支持用户到店堂食、打包带走、线上下单与在线支付，商家通过 Web 管理后台完成商品、订单、分类、上下架等全部运营管理动作。

### 1.2 项目范围
- **微信小程序用户端**：用户浏览菜品、加入购物车、选择堂食/打包、下单、微信支付、查看订单、个人中心。
- **FastAPI 后端服务**：提供小程序与管理后台全部接口，负责本地图片存储、微信支付预下单与回调、业务逻辑处理。
- **Web 管理后台（Vue3）**：商家管理员登录后管理菜品分类、菜品、订单、查看数据看板。
- **MySQL 8.0 数据库**：持久化用户、分类、菜品、购物车、订单、订单详情数据。

### 1.3 不在范围内（明确排除，不实现）
- 不接入任何 OSS / COS / 第三方对象存储，所有图片由 FastAPI 后端本地存储。
- 不实现配送地址、骑手配送、平台抽成、营销优惠券、会员积分、评价评论等需求外功能。
- 不实现多门店、多商家、连锁分店管理。
- 不实现 PC 客户端、App、H5 独立站。

---

## 二、产品目标与定位

### 2.1 产品目标
1. 为餐饮门店提供低成本的数字化点单工具，支持堂食与打包两种核心用餐模式。
2. 通过小程序实现用户免注册快速下单、微信支付闭环。
3. 为商家提供可视化后台，实现商品与订单的零门槛管理。

### 2.2 产品定位
- **用户侧**：轻量、免注册、即开即用的点餐小程序。
- **商家侧**：简单直观的 Web 管理后台，覆盖商品上架到订单履约全流程。
- **系统侧**：本地化部署、轻架构（小程序 + FastAPI + MySQL），无第三方存储依赖。

### 2.3 核心价值
| 角色 | 核心价值 |
|------|----------|
| 用户 | 免注册、快速点单、堂食/打包自由切换、微信支付便捷 |
| 商家 | 商品自主管理、订单实时可见、数据看板辅助经营决策 |
| 系统 | 架构简单、无云存储成本、易部署易维护 |

---

## 三、整体架构与技术方案

### 3.1 架构分层
```
┌─────────────────────┐      ┌─────────────────────┐
│  微信小程序用户端     │      │   Web 管理后台(Vue3)  │
│  WXML/WXSS/JS       │      │   (管理员使用)        │
└─────────┬───────────┘      └─────────┬───────────┘
          │ HTTPS/JSON API              │ HTTPS/JSON API
          ▼                             ▼
┌─────────────────────────────────────────────────┐
│            FastAPI 后端服务 (Python)              │
│  - 路由层(API Router)  - 业务层(Service)          │
│  - ORM 层(SQLAlchemy)  - 本地文件存储模块          │
│  - 微信支付模块(预下单/回调)                       │
└─────────────────────┬───────────────────────────┘
                      │ JDBC/SQLAlchemy
                      ▼
┌─────────────────────────────────────────────────┐
│            MySQL 8.0 数据库                       │
│  用户表/分类表/菜品表/购物车表/订单表/订单详情表    │
└─────────────────────────────────────────────────┘
```

### 3.2 技术栈（强制约束）
| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 客户端 | 微信原生小程序（WXML / WXSS / JS） | 不使用 uni-app / Taro，原生开发 |
| 后端 | Python FastAPI + SQLAlchemy ORM | 异步框架，ORM 操作 MySQL |
| 数据库 | MySQL 8.0 | 字符集 utf8mb4，InnoDB 引擎 |
| 管理后台 | Web 端 Vue3 基础管理界面 | 仅管理员使用，基础 CRUD 界面即可 |
| 文件方案 | FastAPI 后端本地目录存储 | 不使用 OSS/COS/任何云存储 |
| 支付能力 | 微信小程序支付（JSAPI 支付） | 微信支付商户号 + 小程序 AppID |

### 3.3 文件存储方案（关键约束）
- 所有菜品图片、用户头像等文件 **统一保存在 FastAPI 后端服务器本地磁盘目录**（例如 `backend/uploads/`）。
- 数据库中对应字段 **仅存储可访问的相对/绝对路径字符串**（如 `/uploads/dishes/2026/07/abc.jpg`）。
- 后端需提供静态文件访问路由（如 `/static/...`）供小程序与后台读取图片。
- 禁止任何 OSS、COS、七牛、又拍云等第三方存储调用。

### 3.4 接口规范
- 协议：HTTPS（生产），HTTP（本地开发）。
- 数据格式：JSON。
- 鉴权：小程序端使用 `token`（自定义登录态，由后端下发）；后台使用管理员 `token`（JWT 或 Session 二选一，PRD 约定为后端自定义 token 校验）。
- 统一响应结构：`{ "code": 0, "msg": "success", "data": {...} }`，`code != 0` 表示业务失败。

---

## 四、角色与权限说明

### 4.1 角色列表
| 角色 | 标识 | 入口 | 说明 |
|------|------|------|------|
| 普通用户 | `user` | 微信小程序 | 浏览菜品、下单、支付、查看订单 |
| 商家管理员 | `admin` | Web 管理后台 | 登录后台，管理商品/分类/订单/看板 |

### 4.2 权限矩阵
| 功能 | 普通用户 | 商家管理员 |
|------|----------|------------|
| 微信登录 | ✓ | ✗ |
| 浏览菜品/分类 | ✓ | ✓（后台可见全部含下架） |
| 购物车操作 | ✓ | ✗ |
| 下单/支付 | ✓ | ✗ |
| 查看我的订单 | ✓（仅本人） | ✗ |
| 管理员登录 | ✗ | ✓ |
| 分类增删改查 | ✗ | ✓ |
| 菜品增删改查/上下架/图片 | ✗ | ✓ |
| 订单查看（全部） | ✗ | ✓ |
| 订单状态修改 | ✗（仅可取消本人未支付单） | ✓ |
| 数据看板 | ✗ | ✓ |

### 4.3 权限约束（严格）
1. 仅管理员可操作商品与订单状态，用户端无管理权限。
2. 普通用户仅能查看与操作本人的订单数据。
3. 管理员默认权限为超级管理员，不区分多角色、不细分权限。

---

## 五、整体业务流程图

### 5.1 用户点餐支付主流程
```
小程序启动
   │
   ├─ 微信授权登录 ──► 获取 OpenID/昵称/头像 ──► 后端自动注册/登录 ──► 返回 token
   │
   ▼
首页（店铺信息 + 分类 + 热门菜品）
   │
   ▼
菜品分类页（左分类 / 右菜品列表）
   │  点击「加入购物车」
   ▼
购物车（增减数量 / 清空 / 实时总价）
   │  点击「去结算」
   ▼
下单确认页 ──► 选择用餐模式 [堂食 | 打包带走]（必选，二选一）
   │             堂食：不填地址；打包：标注打包类型
   ▼
提交订单 ──► 后端创建订单（状态：待支付）──► 调用微信支付预下单
   │
   ▼
微信支付收银台 ──► 支付成功回调 ──► 订单状态：待出餐
                        │
                        └─ 支付失败 ──► 保留待支付订单，可重新支付
   │
   ▼
我的订单（待支付 / 待出餐 / 已完成 / 已取消）
```

### 5.2 商家履约流程
```
管理员登录后台
   │
   ▼
订单管理 ──► 查看全部订单（按状态筛选）
   │  查看订单详情（菜品/数量/总价/用餐方式/状态）
   ▼
商家修改状态：待出餐 ──► 已出餐/已完成
   │
   ▼
数据看板：今日订单数 / 今日销售额 / 在售菜品数
```

### 5.3 商品管理流程
```
管理员登录后台
   │
   ├─ 分类管理：新增 / 编辑 / 删除 / 排序
   │
   └─ 菜品管理：新增（上传图片至本地+填信息） / 编辑（可替换图）
                 / 上架下架（下架后小程序不可见） / 删除
```

---

## 六、小程序用户端详细功能需求

> 以下按页面逐一描述，包含页面元素、按钮、交互逻辑、接口依赖。

### 6.1 微信登录模块（自动授权）
- **触发时机**：小程序首次启动或 token 失效时自动执行。
- **交互流程**：
  1. 调用 `wx.login()` 获取 `code`。
  2. 调用后端登录接口，传入 `code`。
  3. 后端用 `code` 向微信换取 `OpenID`，并获取用户昵称、头像（通过 `wx.getUserProfile` 或新版本头像昵称填写组件获取）。
  4. 后端自动注册（OpenID 不存在时）或登录（已存在时），返回 `token` 与用户信息。
  5. 小程序本地缓存 `token`，后续请求携带。
- **无手动注册页**：整个过程用户无感知，无需填写任何表单。
- **异常**：登录失败给出 Toast 提示，并允许重试。

### 6.2 首页模块
- **页面元素**：
  - 顶部：店铺名称（大标题）、店铺简介（副标题/描述）。
  - 中部：菜品分类列表（横向滚动或宫格展示，点击跳转/联动分类页）。
  - 下部：热门菜品列表（展示 2~4 个热门菜品卡片，含图片、名称、价格）。
- **交互逻辑**：
  - 点击分类 → 进入「菜品分类与列表页」并定位到对应分类。
  - 点击热门菜品 → 进入菜品详情或分类页对应位置。
- **数据来源**：店铺信息（可配置常量或接口）、分类列表接口、热门菜品接口（取部分在售菜品）。

### 6.3 菜品分类与菜品列表页
- **布局**：左侧分类栏（竖向列表）+ 右侧菜品列表（滚动）。
- **左侧分类栏**：
  - 展示全部分类（来自分类接口）。
  - 点击分类 → 右侧联动展示该分类下的菜品，选中态高亮。
- **右侧菜品列表**（每个菜品卡片包含）：
  - 菜品图片（来自菜品 image 路径，经后端静态路由访问）。
  - 菜品名称。
  - 菜品价格（单位：元，保留两位小数）。
  - 菜品简介（一行或两行省略）。
  - 「加入购物车」按钮（或 +/- 步进器）。
- **交互逻辑**：
  - 点击「加入购物车」→ 调用购物车接口添加，按钮处出现数量步进器（− 数量 +）。
  - 下架菜品（status=下架）**不展示**在列表中。
  - 实时更新购物车角标数量（顶部购物车 icon）。

### 6.4 购物车功能
- **入口**：底部 TabBar 或首页悬浮购物车图标。
- **页面元素**：
  - 菜品列表：图片、名称、单价、数量步进器（−/+）、小计。
  - 底部结算栏：合计总价（实时计算）、「去结算」按钮、「清空购物车」按钮。
- **交互逻辑**：
  - 增加/减少数量：调用购物车接口同步，`+` 数量加 1，`−` 减 1（减到 0 移除该菜品）。
  - 清空购物车：二次确认弹窗 → 调用清空接口。
  - 合计总价：所有菜品 `单价 × 数量` 实时求和。
  - 去结算：跳转下单确认页。
- **约束**：下架菜品不可加入购物车；若购物车中菜品被下架，进入购物车页时应提示并移除。

### 6.5 下单核心功能（重点）
- **入口**：购物车「去结算」。
- **页面元素**：
  - 订单商品清单：菜品、单价、数量、小计。
  - 用餐模式选择：单选控件，**堂食 / 打包带走 二选一，必选**（默认可不预选，提交时校验必须选其一）。
  - 堂食：无需填写任何地址/联系方式字段（仅提交订单）。
  - 打包：默认支持打包属性，订单标注打包类型（无需额外必填信息，按需求仅标注类型）。
  - 订单总价（含明细小计合计）。
  - 「提交订单」按钮。
- **交互逻辑**：
  1. 未选用餐模式提交 → Toast 提示「请选择堂食或打包」。
  2. 调用下单接口 → 后端创建订单（状态：待支付），返回 `order_id` / `order_no` 与支付参数。
  3. 成功后自动跳转微信支付收银台。
- **业务规则**：
  - 堂食订单不存储地址字段。
  - 打包订单 `dining_mode` 标注为「打包」。

### 6.6 微信支付功能
- **触发**：下单成功后，后端返回微信支付预下单参数（`timeStamp` / `nonceStr` / `package` / `signType` / `paySign`）。
- **交互**：
  - 调用 `wx.requestPayment()` 唤起微信支付。
  - **支付成功**：后端回调通知订单状态变更为「待出餐」；前端跳转「我的订单」并提示支付成功。
  - **支付失败/取消**：保留「待支付」订单，用户可在「我的订单 - 待支付」中点击「重新支付」再次唤起收银台。
- **异常**：支付失败给出 Toast，不关闭订单，可重试。

### 6.7 我的订单模块
- **订单状态分类**（顶部 Tab 切换）：
  - 待支付、待出餐、已完成、已取消。
- **列表项展示**：订单号、下单时间、用餐方式（堂食/打包图标或文字）、状态标签、总价、缩略菜品图。
- **订单详情页**（点击列表项进入）：
  - 菜品清单（名称、单价、数量、小计）。
  - 下单时间。
  - 用餐方式（堂食/打包）。
  - 订单状态。
  - 订单总价。
  - 待支付订单提供「重新支付」「取消订单」按钮。
- **交互逻辑**：
  - 取消订单（仅待支付）：二次确认 → 调用取消接口 → 状态变「已取消」。
  - 重新支付（仅待支付）：调用支付预下单 → 唤起收银台。

### 6.8 个人中心
- **页面元素**：
  - 用户头像（圆形，来自登录获取的 avatar 本地路径或微信头像）。
  - 用户昵称。
  - 「全部订单」入口（跳转我的订单）。
  - （可选展示）店铺信息区。
- **交互**：点击「全部订单」→ 进入我的订单页（默认全部状态）。

---

## 七、Web 管理后台详细功能需求

> 后台使用 Vue3 基础管理界面，管理员账号密码登录后使用。

### 7.1 管理员登录
- **页面元素**：账号输入框、密码输入框、登录按钮。
- **交互**：
  - 提交 → 调用管理员登录接口 → 成功返回 `token`，前端存储并跳转首页；失败提示错误。
  - 未登录访问受保护页面 → 重定向至登录页。
- **权限**：登录后权限为超级管理员，可访问全部后台功能。

### 7.2 菜品分类管理
- **页面元素**：分类列表（名称、排序值、操作列）。
- **功能**：
  1. **新增分类**：弹窗输入分类名称、排序值 → 提交。
  2. **编辑分类**：修改名称、排序 → 提交。
  3. **删除分类**：二次确认 → 删除（需处理该分类下是否有菜品，如无菜品可直接删；如有菜品，PRD 约定：删除前需先转移或禁止删除，具体以接口校验返回提示为准）。
  4. **分类排序**：通过排序值字段调整显示顺序。
- **接口依赖**：分类增删改查接口。

### 7.3 菜品商品管理（核心重点）
- **列表页元素**：菜品图片缩略图、名称、价格、所属分类、状态（在售/下架）、操作列（编辑、上下架、删除）。
- **功能**：
  1. **新增菜品**：
     - 上传菜品图片（前端选图 → 调图片上传接口 → 后端保存至本地目录 → 返回路径）。
     - 填写：菜名、价格（数字）、简介（文本）、选择所属分类（下拉）。
     - 提交 → 创建菜品（默认在售）。
  2. **编辑菜品**：
     - 修改全部信息（菜名、价格、简介、分类）。
     - 可替换图片（重新上传覆盖）。
     - 提交保存。
  3. **菜品上下架**：
     - 列表「上架/下架」按钮切换 `status`。
     - 下架后小程序端不展示、不可加入购物车。
  4. **删除菜品**：二次确认 → 删除（可同时删除本地图片文件或保留，PRD 约定由后端处理）。
- **关键约束**：所有图片保存在 FastAPI 后端本地目录，数据库仅存访问路径，不使用云存储。

### 7.4 订单管理模块
- **列表页元素**：订单号、用户、下单时间、用餐方式（堂食/打包标注）、总价、状态、操作。
- **功能**：
  1. **查看全部用户订单**：分页列表展示所有订单。
  2. **查看订单详情**：弹窗/新页展示菜品明细、数量、总价、用餐方式、订单状态、下单时间、支付时间。
  3. **商家修改订单状态**：将「待出餐」修改为「已出餐/已完成」（状态流转：待出餐 → 已完成）。
  4. **筛选订单**：按状态（待支付/待出餐/已完成/已取消）筛选；可按时间范围检索（基础支持）。
- **约束**：仅管理员可修改订单状态，用户端无此权限。

### 7.5 基础数据看板
- **展示指标**：
  - 今日订单数（统计当日创建的订单数量）。
  - 今日销售额（统计当日已支付订单的总金额合计）。
  - 在售菜品数量（统计 status=在售 的菜品数）。
- **交互**：页面加载时拉取看板接口，展示数值卡片（可选简单趋势，PRD 不强制图表）。

---

## 八、数据字典与数据库表结构设计

> 数据库：MySQL 8.0，字符集 `utf8mb4`，引擎 `InnoDB`。  
> 所有图片字段统一存储后端本地访问路径（字符串），无云存储字段。  
> 金额字段统一使用 `DECIMAL(10,2)`，单位：元。

### 8.1 用户表 `users`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 用户ID |
| openid | VARCHAR(64) | UNIQUE, NOT NULL | 微信 OpenID |
| nickname | VARCHAR(64) | NULL | 用户昵称 |
| avatar | VARCHAR(255) | NULL | 头像本地访问路径 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NULL ON UPDATE | 更新时间 |

> 说明：用户由微信登录自动注册，无密码字段。

### 8.2 分类表 `categories`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 分类ID |
| name | VARCHAR(64) | NOT NULL | 分类名称 |
| sort_order | INT | DEFAULT 0 | 排序值，越小越靠前 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |

### 8.3 菜品表 `dishes`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 菜品ID |
| name | VARCHAR(128) | NOT NULL | 菜品名称 |
| price | DECIMAL(10,2) | NOT NULL | 价格（元） |
| description | VARCHAR(512) | NULL | 菜品简介 |
| image | VARCHAR(255) | NULL | 菜品图片本地访问路径 |
| category_id | BIGINT | FK→categories.id, NOT NULL | 所属分类 |
| status | TINYINT | DEFAULT 1 | 1=在售, 0=下架 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NULL ON UPDATE | 更新时间 |

> 约束：下架（`status=0`）菜品小程序不可见、不可加入购物车。

### 8.4 购物车表 `cart`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 购物车项ID |
| user_id | BIGINT | FK→users.id, NOT NULL | 用户ID |
| dish_id | BIGINT | FK→dishes.id, NOT NULL | 菜品ID |
| quantity | INT | NOT NULL, DEFAULT 1 | 数量 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NULL ON UPDATE | 更新时间 |

> 说明：以 `(user_id, dish_id)` 唯一确定购物车项；数量为 0 时移除。

### 8.5 订单表 `orders`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 订单ID |
| order_no | VARCHAR(64) | UNIQUE, NOT NULL | 订单编号（业务唯一） |
| user_id | BIGINT | FK→users.id, NOT NULL | 用户ID |
| total_amount | DECIMAL(10,2) | NOT NULL | 订单总价（元） |
| dining_mode | TINYINT | NOT NULL | 1=堂食, 2=打包带走 |
| status | TINYINT | NOT NULL, DEFAULT 1 | 1=待支付,2=待出餐,3=已完成,4=已取消 |
| pay_status | TINYINT | DEFAULT 0 | 0=未付,1=已付 |
| address | VARCHAR(255) | NULL | 堂食为空；打包按需求标注类型（可空） |
| expire_at | DATETIME | NULL | 未支付订单超时关闭时间 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 下单时间 |
| paid_at | DATETIME | NULL | 支付时间 |
| updated_at | DATETIME | NULL ON UPDATE | 更新时间 |

> 约束：`dining_mode` 明确区分堂食/打包，后台列表标注；未支付订单超时（`expire_at`）自动关闭为已取消。

### 8.6 订单详情表 `order_items`
| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | BIGINT | PK, AUTO_INCREMENT | 明细ID |
| order_id | BIGINT | FK→orders.id, NOT NULL | 订单ID |
| dish_id | BIGINT | FK→dishes.id, NOT NULL | 菜品ID（快照关联） |
| dish_name | VARCHAR(128) | NOT NULL | 下单时菜品名称快照 |
| price | DECIMAL(10,2) | NOT NULL | 下单时单价快照 |
| quantity | INT | NOT NULL | 数量 |
| subtotal | DECIMAL(10,2) | NOT NULL | 小计 = price × quantity |

> 说明：菜品名称、单价下单时落快照，避免菜品后续改价/下架影响历史订单。

### 8.7 索引建议
- `users(openid)` 唯一索引。
- `orders(order_no)` 唯一索引；`orders(user_id, status)` 普通索引。
- `dishes(category_id, status)` 普通索引。
- `cart(user_id, dish_id)` 唯一索引。
- `order_items(order_id)` 普通索引。

---

## 九、后端接口需求清单

> 基础前缀约定：`/api/v1`。小程序端前缀 `/api/v1/client`，后台前缀 `/api/v1/admin`。  
> 鉴权：`Authorization: Bearer <token>` 请求头（后台登录与管理员操作接口除外）。

### 9.1 小程序登录授权
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/client/auth/login` | POST | 接收 `code`，后端换 OpenID、获取昵称头像，自动注册/登录，返回 `token` + 用户信息 |

### 9.2 分类接口（小程序端只读）
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/client/categories` | GET | 获取全部分类（按 sort_order 排序） |
| `/api/v1/client/dishes/hot` | GET | 获取热门/在售菜品列表（首页用） |

### 9.3 菜品接口（小程序端只读）
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/client/dishes` | GET | 按分类获取在售菜品列表（下架不返回） |
| `/api/v1/client/dishes/{id}` | GET | 菜品详情 |

### 9.4 购物车接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/client/cart` | GET | 获取当前用户购物车列表 |
| `/api/v1/client/cart/add` | POST | 添加菜品到购物车（user_id, dish_id, quantity） |
| `/api/v1/client/cart/update` | POST | 修改购物车某菜品数量 |
| `/api/v1/client/cart/clear` | POST | 清空当前用户购物车 |

### 9.5 订单接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/client/orders` | GET | 获取我的订单（可按 status 筛选） |
| `/api/v1/client/orders/{id}` | GET | 订单详情（含菜品明细、用餐方式、状态） |
| `/api/v1/client/orders` | POST | 创建订单（必传 dining_mode：1堂食/2打包），返回 order_id 与支付参数 |
| `/api/v1/client/orders/{id}/cancel` | POST | 取消订单（仅待支付，本人） |
| `/api/v1/client/orders/{id}/repay` | POST | 重新支付预下单（待支付订单） |

### 9.6 微信支付接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/client/pay/prepay` | POST | 微信支付预下单，返回 `wx.requestPayment` 所需参数 |
| `/api/v1/client/pay/notify` | POST | 微信支付异步回调，校验签名，更新订单状态为待出餐、pay_status=已付 |

> 注：`/api/v1/client/pay/notify` 为微信服务器回调地址，需豁免鉴权与 CSRF。

### 9.7 管理员后台接口
| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/v1/admin/auth/login` | POST | 管理员账号密码登录，返回 `token` |
| `/api/v1/admin/categories` | GET/POST | 分类列表 / 新增分类 |
| `/api/v1/admin/categories/{id}` | PUT/DELETE | 编辑 / 删除分类 |
| `/api/v1/admin/dishes` | GET/POST | 菜品列表（含下架）/ 新增菜品 |
| `/api/v1/admin/dishes/{id}` | PUT/DELETE | 编辑（含替换图片）/ 删除菜品 |
| `/api/v1/admin/dishes/{id}/toggle` | POST | 菜品上架/下架切换 |
| `/api/v1/admin/upload` | POST | 图片上传，保存到本地目录，返回访问路径 |
| `/api/v1/admin/orders` | GET | 订单列表（支持 status 筛选、时间筛选） |
| `/api/v1/admin/orders/{id}` | GET | 订单详情 |
| `/api/v1/admin/orders/{id}/status` | POST | 修改订单状态（待出餐 → 已完成） |
| `/api/v1/admin/dashboard` | GET | 看板数据：今日订单数、今日销售额、在售菜品数 |

### 9.8 静态资源
| 接口 | 方法 | 说明 |
|------|------|------|
| `/static/{path}` | GET | 访问后端本地存储的图片等静态文件 |

---

## 十、业务规则与约束说明

1. **下架菜品不可见**：`dishes.status=0` 的菜品，小程序端列表、首页热门、搜索均不展示，且不可加入购物车；后台可见并可管理。
2. **堂食/打包区分**：每个订单 `dining_mode` 必为 1（堂食）或 2（打包）；堂食不存储地址；后台订单列表与详情明确标注用餐方式。
3. **未支付订单超时关闭**：创建订单时设置 `expire_at`（如 15 分钟或 30 分钟后）；后端定时任务或访问时校验，超时未支付订单自动置为「已取消」（status=4）。
4. **图片本地存储**：所有图片（菜品图、头像）由 FastAPI 后端保存到本地磁盘目录，数据库仅存访问路径，禁止任何第三方云存储。
5. **权限隔离**：仅管理员可操作商品（增删改查、上下架）与订单状态；用户端无管理权限，仅可管理本人订单（取消未支付单、重新支付）。
6. **订单状态机**：
   - 待支付(1) →（支付成功）→ 待出餐(2)
   - 待支付(1) →（用户取消/超时）→ 已取消(4)
   - 待出餐(2) →（商家操作）→ 已完成(3)
   - 已完成(3)、已取消(4) 为终态。
7. **支付幂等**：微信支付回调需做幂等处理，避免重复通知导致重复更新。
8. **购物车一致性**：结算下单时若菜品已下架，应提示并阻止下单。

---

## 十一、非功能需求

### 11.1 性能需求
- 小程序首页、分类页接口响应时间 P95 < 500ms。
- 列表接口支持分页，单页默认 20 条。
- MySQL 关键查询需有索引（见 8.7）。
- 图片静态访问走本地文件，单图大小建议 < 2MB，上传时后端可做压缩/尺寸限制。

### 11.2 兼容性需求
- 微信小程序：基础库版本覆盖近 2 年主流版本；适配 iOS 与 Android。
- 管理后台 Web：兼容 Chrome / Edge 最新两个大版本；分辨率适配 1366×768 及以上。
- 数据库：MySQL 8.0（明确版本，不使用 5.7 及以下）。

### 11.3 安全需求
- 小程序 `token` 与后台 `token` 均需有效期管理，敏感操作校验登录态。
- 微信支付回调必须校验微信签名，防止伪造通知。
- 后台接口需做管理员权限校验，防止越权访问。
- 图片上传限制文件类型（jpg/png）与大小，防止恶意文件上传。

### 11.4 可靠性需求
- 订单创建与支付状态更新需保证一致性（事务）。
- 微信支付异步回调需确认处理成功后再返回 `success` 给微信，避免重复推送。
- 本地图片目录需有备份机制（运维层面）。

### 11.5 可维护性
- 后端代码分层清晰（路由/服务/模型），接口文档使用 FastAPI 自带 Swagger（`/docs`）。
- 数据库表结构通过 SQLAlchemy 模型 + 迁移脚本（Alembic 可选）管理。
- 配置（数据库、微信支付密钥、本地存储路径）通过环境变量/配置文件管理，不硬编码。

### 11.6 部署说明（概要）
- 后端：FastAPI（uvicorn/gunicorn）部署，本地 `uploads/` 目录需持久化挂载。
- 数据库：MySQL 8.0 独立部署，开启外网/内网访问白名单。
- 小程序：微信公众平台配置服务器域名（HTTPS）包含后端 API 与静态资源域名。
- 后台：Vue3 构建为静态资源，Nginx 托管，API 反向代理至 FastAPI。

---

## 附录：需求确认与开发交付清单
- [ ] 小程序端 8 大模块（登录/首页/分类列表/购物车/下单/支付/我的订单/个人中心）
- [ ] 后台 5 大模块（登录/分类/菜品/订单/看板）
- [ ] 后端接口清单全部实现（含微信支付预下单与回调）
- [ ] 6 张核心表按 8.1~8.6 建表
- [ ] 本地图片存储与静态访问路由
- [ ] 业务规则 1~8 全部落地

> 本文档严格依据需求生成，未新增、未删减、未脑补需求外功能。
